package src;

import javax.swing.*;

public class Main extends JFrame {
    public Main() {
        setTitle("Enhanced Swing Application");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setSize(600, 400);
        setLocationRelativeTo(null);  // Center the window

        // Add a more complex panel (MyPanel)

        setVisible(true);
    }
}


